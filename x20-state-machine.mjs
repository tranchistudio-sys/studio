const transitions = { QUEUED: ["ANALYZING"], ANALYZING: ["CODING"], CODING: ["TESTING"], TESTING: ["REVIEW_READY"], REVIEW_READY: ["PREVIEW_READY"], PREVIEW_READY: ["WAITING_MERGE_APPROVAL"], WAITING_MERGE_APPROVAL: ["MERGED"], MERGED: ["WAITING_DEPLOY_APPROVAL"], WAITING_DEPLOY_APPROVAL: ["DEPLOYING"], DEPLOYING: ["HEALTH_CHECK"], HEALTH_CHECK: ["COMPLETED", "ROLLED_BACK"] };
export function transition(task, next, context = {}) { if (!transitions[task.status]?.includes(next)) throw new Error(`INVALID_TRANSITION:${task.status}->${next}`); if (next === "DEPLOYING" && context.approved !== true) throw new Error("DEPLOY_APPROVAL_REQUIRED"); if (next === "DEPLOYING" && context.mergedSha !== task.commitSha) throw new Error("MERGED_SHA_REQUIRED"); if (task.status === "CODING" && next === "DEPLOYING") throw new Error("CODING_CANNOT_DEPLOY"); return { ...task, status: next }; }
export const allowedTransitions = transitions;
